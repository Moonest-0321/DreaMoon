# V4.2 測試報告

> 日期：2026-09-01
>
> 平台：macOS 26.5、Xcode 26.6（17F113）

## 結果

- Debug 與 Release macOS 組態建置：通過。
- 完整單元／整合測試：27 項通過、0 項失敗。
- V4.2 測試：8 項，包含全新 V2 store 重開與實際 V1→V2 檔案型 store 遷移。
- 既有回歸：19 項，包含故事標籤、每節註記、V5 主 store、物品副本與跨模組行為。
- `git diff --check`：通過。

## V4.2 覆蓋

- 故事背景建立、儲存、重載與重複 ensure。
- 四類故事線、多筆前傳／支線／後記，以及同一本書禁止重複建立主線。
- 主線至少兩個有序階段，以及非主線不得建立階段的失敗情境。
- 四種大綱狀態、手動順序與檔案型 store 重新開啟後的穩定性。
- 敘事大綱與時間軸查詢取得同一個 `OutlineItem`，修改後不產生副本。
- 沒有正文、節、故事標籤或舊時間軸資料時仍可建立大綱。
- V1 故事標籤 UUID／錨點及每節註記在 V2 遷移後不變，第二次開啟不重複建立資料。

## 尚未驗證

- 已啟動使用隔離 store 的測試版，但系統同時存在多個相同 bundle identifier 的 Sailune 執行個體，介面自動化無法可靠辨識隔離視窗；為避免誤寫正式使用者資料，本輪沒有執行會建立內容的 UI 操作，因此人工 UI 冒煙測試仍未完成。
- 正式簽章／封裝不屬於本輪自動測試結果。
- PRD 明列的非目標與待確認項目（刪除／復原、匯出、搜尋、篩選、正文嫁接等）未納入驗證。
